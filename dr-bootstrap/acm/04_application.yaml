apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: hub-infra-operators
  namespace: open-cluster-management
spec:
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
  selector:
    matchExpressions:
    - key: app
      operator: In
      values:
      - hub-infra-operators
